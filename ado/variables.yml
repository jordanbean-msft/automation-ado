parameters:
  - name: adoAccountName

variables:
  pathToAdoDirectory: $(System.DefaultWorkingDirectory)/${{ parameters.adoAccountName }}/ado
  pathToInfrastructureDirectory: $(System.DefaultWorkingDirectory)/${{ parameters.adoAccountName }}/infra
  pathToRunbooksDirectory: $(System.DefaultWorkingDirectory)/${{ parameters.adoAccountName }}/runbooks
  pathToInfrastructureEnvironmentDirectory: $(System.DefaultWorkingDirectory)/${{ parameters.adoAccountName }}/infra/env